info:
  name: POST Log in as existing user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/users/login
  body:
    type: json
    data: |-
      {
        "email":"test+seed2@email.com",
        "password":"bananas1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
